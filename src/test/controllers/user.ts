import * as mocha from 'mocha';
import * as assert from 'power-assert';

describe('test ', () => {
    context('testMethod()', () => {
        it('it is ok.', () => {
            assert.ok(true);
        });
    });
});

